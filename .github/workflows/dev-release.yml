name: Dev Release

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "Pull request number to publish a dev release from"
        required: true
        type: number
      dry_run:
        description: "Run the full build pipeline without publishing to npm"
        required: false
        type: boolean
        default: false

defaults:
  run:
    shell: bash -l {0}

env:
  CACHE_NAME: node-modules-cache
  PR_COMMENT_MARKER: "<!-- backpack-dev-release -->"

permissions: {}

jobs:
  SecurityCheck:
    name: Validate PR and permissions
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    outputs:
      pr_sha: ${{ steps.validate.outputs.pr_sha }}
    steps:
      - name: Checkout for actions
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          sparse-checkout: .github/actions

      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          app-id: ${{ vars.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}

      - name: Validate PR security
        id: validate
        uses: ./.github/actions/pr-security-validation
        with:
          pr_number: ${{ inputs.pr_number }}
          github_token: ${{ steps.app-token.outputs.token }}

      - name: Add or update starting comment on PR
        if: success()
        uses: ./.github/actions/upsert-pr-comment
        with:
          pr_number: ${{ inputs.pr_number }}
          marker: ${{ env.PR_COMMENT_MARKER }}
          github_token: ${{ steps.app-token.outputs.token }}
          body: |
            ${{ env.PR_COMMENT_MARKER }}
            ### Dev Release Started

            Building and publishing a dev release. The result will be posted here when the workflow completes.

            See the [workflow run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) for live progress.

            Triggered by @${{ github.actor }}.

      - name: Add or update failure comment on PR
        if: failure()
        uses: ./.github/actions/upsert-pr-comment
        with:
          pr_number: ${{ inputs.pr_number }}
          marker: ${{ env.PR_COMMENT_MARKER }}
          github_token: ${{ steps.app-token.outputs.token }}
          body: |
            ${{ env.PR_COMMENT_MARKER }}
            ### Dev Release Failed

            The dev release workflow failed a security check. See the [workflow run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}) for details.

            Triggered by @${{ github.actor }}.

  DevRelease:
    name: Build and publish dev release
    runs-on: ubuntu-latest
    # Dry runs do not need maintainer approval because they never publish.
    # Real publishes still go through the Publishing environment so the
    # backpack-web team approves the npm release. The expression must put
    # 'Publishing' on the truthy branch — `cond && '' || 'X'` always yields
    # 'X' because '' is falsy in GitHub Actions expressions.
    environment: ${{ !inputs.dry_run && 'Publishing' || '' }}
    needs: [SecurityCheck]
    permissions:
      contents: read
    outputs:
      dev_version: ${{ steps.version.outputs.dev_version }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ needs.SecurityCheck.outputs.pr_sha }}
          persist-credentials: false
          fetch-depth: 0

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: ".nvmrc"
          registry-url: 'https://registry.npmjs.org'

      - name: Cache npm dependencies
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        id: npm-cache
        with:
          path: |
            node_modules/
            packages/node_modules/
          key: ${{ env.CACHE_NAME }}-${{ hashFiles('package-lock.json', 'packages/package-lock.json') }}

      - name: Install dependencies
        if: ${{ steps.npm-cache.outputs.cache-hit != 'true' }}
        run: npm ci

      - name: Determine dev version
        id: version
        run: |
          BASE_VERSION=$(git describe --tags --abbrev=0 --exclude='*-*' HEAD)
          DEV_VERSION="${BASE_VERSION}-dev-v${GITHUB_RUN_ID}.${GITHUB_RUN_ATTEMPT}"
          echo "dev_version=${DEV_VERSION}" >> "$GITHUB_OUTPUT"
          echo "Dev version: ${DEV_VERSION}"

      - name: Build
        run: npm run build

      # Update the version in dist/package.json directly rather than running
      # `npm version`, which would execute any preversion / version /
      # postversion lifecycle scripts present in the PR's package.json.
      # Always run on dry runs too so the stamping logic itself is
      # exercised before a real publish depends on it.
      - name: Stamp dev version into dist/package.json
        working-directory: packages/backpack-web/dist
        run: |
          node -e "const fs=require('fs'); const p=require('./package.json'); p.version=process.env.DEV_VERSION; fs.writeFileSync('./package.json', JSON.stringify(p, null, 2) + '\n');"
          node -e "console.log('dist/package.json version:', require('./package.json').version);"
        env:
          DEV_VERSION: ${{ steps.version.outputs.dev_version }}

      # `--ignore-scripts` prevents npm from running prepack / prepare /
      # prepublishOnly / postpack / postpublish hooks from the PR's
      # package.json with NPM_TOKEN in scope. The published artifact is
      # already fully built by the Build step above, so no script needs to
      # run at publish time.
      - name: Publish dev release to npm
        if: ${{ inputs.dry_run != true }}
        working-directory: packages/backpack-web/dist
        run: npm publish --ignore-scripts --tag dev
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  ReportResults:
    name: Report results on PR
    runs-on: ubuntu-latest
    needs: [SecurityCheck, DevRelease]
    if: always() && needs.SecurityCheck.result == 'success'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout for actions
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          sparse-checkout: .github/actions

      - uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        id: app-token
        with:
          app-id: ${{ vars.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}

      - name: Build comment body
        id: build-body
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        env:
          DEV_VERSION: ${{ needs.DevRelease.outputs.dev_version }}
          PR_SHA: ${{ needs.SecurityCheck.outputs.pr_sha }}
          BUILD_RESULT: ${{ needs.DevRelease.result }}
          DRY_RUN: ${{ inputs.dry_run }}
          RUN_ID: ${{ github.run_id }}
          REPO: ${{ github.repository }}
          PR_COMMENT_MARKER: ${{ env.PR_COMMENT_MARKER }}
        with:
          script: |
            const marker = process.env.PR_COMMENT_MARKER;
            let body;

            if (process.env.BUILD_RESULT === 'success' && process.env.DRY_RUN === 'true') {
              body = [
                marker,
                `### Dev Release Dry Run Successful`,
                '',
                `**Version (not published):** \`${process.env.DEV_VERSION}\``,
                '',
                'The build completed successfully. No package was published to npm.',
                '',
                'Run the workflow again without dry run to publish this version.',
                '',
                `Triggered from commit ${process.env.PR_SHA} by @${context.actor}.`,
              ].join('\n');
            } else if (process.env.BUILD_RESULT === 'success') {
              body = [
                marker,
                `### Dev Release Published`,
                '',
                `**Version:** \`${process.env.DEV_VERSION}\``,
                '',
                '**Install with:**',
                '```',
                `npm install @skyscanner/backpack-web@${process.env.DEV_VERSION}`,
                '```',
                '',
                `You can also use the dev tag to install the latest dev release:`,
                '```',
                'npm install @skyscanner/backpack-web@dev',
                '```',
                '',
                `Published from commit ${process.env.PR_SHA} by @${context.actor}.`,
              ].join('\n');
            } else {
              body = [
                marker,
                `### Dev Release Failed`,
                '',
                `The dev release workflow failed. See the [workflow run](https://github.com/${process.env.REPO}/actions/runs/${process.env.RUN_ID}) for details.`,
                '',
                `Triggered by @${context.actor}.`,
              ].join('\n');
            }

            core.setOutput('body', body);

      - name: Update PR comment with results
        uses: ./.github/actions/upsert-pr-comment
        with:
          pr_number: ${{ inputs.pr_number }}
          marker: ${{ env.PR_COMMENT_MARKER }}
          body: ${{ steps.build-body.outputs.body }}
          github_token: ${{ steps.app-token.outputs.token }}
